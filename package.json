{
    "name": "nft-minter",
    "version": "0.1.0",
    "private": true,
    "dependencies": {
        "@alch/alchemy-web3": "^1.0.1"
    },
    "scripts": {
        "start": "cd minter-starter-files && npm start",
        "build": "cd minter-starter-files && npm install && npm run-script build"
    },
    "browserslist": {
        "production": [
            ">0.2%",
            "not dead",
            "not op_mini all"
        ],
        "development": [
            "last 1 chrome version",
            "last 1 firefox version",
            "last 1 safari version"
        ]
    },
    "devDependencies": {
        "dotenv": "^8.6.0"
    }
}